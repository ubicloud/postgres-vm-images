#!/bin/bash
set -uexo pipefail

# Standard flavor setup
# This flavor uses only the common PostgreSQL extensions
# No additional setup required

echo "=== Standard flavor setup ==="
echo "No additional packages to install for standard flavor"
echo "=== Standard flavor setup complete ==="
