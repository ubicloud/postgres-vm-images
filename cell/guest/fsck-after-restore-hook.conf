# A clone wakes with the template's wall clock, behind the times the volume's
# superblock recorded on its last mount, and e2fsck treats times in the future
# as a problem to repair. So fsck waits for the hook to set the clock.
[Unit]
After=cell-restore-hook.service
